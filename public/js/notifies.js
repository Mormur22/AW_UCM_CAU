"use strict";

function viewNotify(id) {
    alert("Ver aviso " + id);
}

function assignNotify(id) {
    alert("Asignar aviso " + id);
}

function cancelNotify(id) {
    alert("Cancelar aviso " + id);
}